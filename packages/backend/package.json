{
	"name": "@open-voucher/backend",
	"version": "1.0.0",
	"scripts": {
		"dev": "convex dev",
		"dev:setup": "convex dev --configure --until-success",
		"test": "vitest run",
		"test:watch": "vitest",
		"test:e2e": "bun test tests/convex-test/",
		"check-types": "tsc --noEmit",
		"deploy": "convex deploy",
		"expiring": "convex run dashboard:getExpiringVouchers --prod",
		"expiring:dev": "convex run dashboard:getExpiringVouchers"
	},
	"author": "",
	"license": "ISC",
	"description": "",
	"devDependencies": {
		"@edge-runtime/vm": "^5.0.0",
		"@types/bun": "^1.3.3",
		"@types/node": "^24.3.0",
		"convex-test": "^0.0.41",
		"typescript": "^5.9.2",
		"vitest": "^4.0.15"
	},
	"dependencies": {
		"convex": "^1.31.7",
		"convex-helpers": "^0.1.108",
		"dayjs": "^1.11.19"
	}
}
